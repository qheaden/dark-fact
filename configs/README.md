# Config Directory

This directory contains configuration files that are mounted into dark factory containers.

These files are:
- `opencode-factory-auth.json` - OpenCode authentication credentials
- `opencode-factory.json` - OpenCode configuration
- `claude-code-factory.json` - Claude Code configuration including authentication credentials

All files in this directory are ignored by Git except for this README.md file.