# Config Directory

This directory contains configuration files mounted into the factory container.

These files are:
- `factory-auth.json` - Authentication credentials
- `factory-config.json` - Factory configuration

All files in this directory are ignored by Git except for this README.md file.
