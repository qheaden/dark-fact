# Shared Skills

Store skills that should be available to every dark factory in this directory. Each factory bind-mounts `shared-skills/` at runtime, so skills added or changed here become available immediately without rebuilding any factory image.
